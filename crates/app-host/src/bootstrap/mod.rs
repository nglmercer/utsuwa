//! Native host composition root. See the submodules for the service
//! bundle (`services`), the agent runtime (`agent`), and the durable
//! task authority (`tasks`).

pub mod agent;
pub mod services;
pub mod tasks;
